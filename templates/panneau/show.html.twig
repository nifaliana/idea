{% extends 'base.html.twig' %}

{% block title %}Panneau{% endblock %}

{% block body %}
    <h1>Panneau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panneau.id }}</td>
            </tr>
            <tr>
                <th>Format</th>
                <td>{{ panneau.format }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ panneau.unit }}</td>
            </tr>
            <tr>
                <th>Lat</th>
                <td>{{ panneau.lat }}</td>
            </tr>
            <tr>
                <th>Lng</th>
                <td>{{ panneau.lng }}</td>
            </tr>
            <tr>
                <th>TarifImpression</th>
                <td>{{ panneau.tarifImpression }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ panneau.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panneau_index') }}">back to list</a>

    <a href="{{ path('panneau_edit', {'id': panneau.id}) }}">edit</a>

    {{ include('panneau/_delete_form.html.twig') }}
{% endblock %}
