{% extends 'base.html.twig' %}

{% block title %}Payement index{% endblock %}

{% block body %}
    <h1>Payement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Reference</th>
                <th>Somme</th>
                <th>ModePayement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for payement in payements %}
            <tr>
                <td>{{ payement.id }}</td>
                <td>{{ payement.reference }}</td>
                <td>{{ payement.somme }}</td>
                <td>{{ payement.modePayement }}</td>
                <td>
                    <a href="{{ path('payement_show', {'id': payement.id}) }}">show</a>
                    <a href="{{ path('payement_edit', {'id': payement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('payement_new') }}">Create new</a>
{% endblock %}
