{% extends 'base.html.twig' %}

{% block title %}Confection{% endblock %}

{% block body %}
    <h1>Confection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ confection.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ confection.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('confection_index') }}">back to list</a>

    <a href="{{ path('confection_edit', {'id': confection.id}) }}">edit</a>

    {{ include('confection/_delete_form.html.twig') }}
{% endblock %}
