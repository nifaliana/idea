{% extends 'base.html.twig' %}

{% block title %}PanneauOffre{% endblock %}

{% block body %}
    <h1>PanneauOffre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panneau_offre.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ panneau_offre.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panneau_offre_index') }}">back to list</a>

    <a href="{{ path('panneau_offre_edit', {'id': panneau_offre.id}) }}">edit</a>

    {{ include('panneau_offre/_delete_form.html.twig') }}
{% endblock %}
