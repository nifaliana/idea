{% extends 'base.html.twig' %}

{% block title %}PanneauOffre index{% endblock %}

{% block body %}
    <h1>PanneauOffre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for panneau_offre in panneau_offres %}
            <tr>
                <td>{{ panneau_offre.id }}</td>
                <td>{{ panneau_offre.price }}</td>
                <td>
                    <a href="{{ path('panneau_offre_show', {'id': panneau_offre.id}) }}">show</a>
                    <a href="{{ path('panneau_offre_edit', {'id': panneau_offre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('panneau_offre_new') }}">Create new</a>
{% endblock %}
