<form method="post" action="{{ path('panneau_offre_delete', {'id': panneau_offre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panneau_offre.id) }}">
    <button class="btn">Delete</button>
</form>
